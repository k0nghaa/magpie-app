# PRD: 매그파이 (Magpie) — 아침 영어 회화 습관 앱

| 항목 | 내용 |
|---|---|
| 문서 버전 | v0.1 (2026-08-28) |
| 용도 | 개인 사이드프로젝트 설계용 |
| 플랫폼 | iOS + Android 크로스플랫폼 (React Native / Expo) |
| 상태 | 초안 — 미결정 사항은 §10 참조 |

---

## 1. 배경 & 문제 정의

영어 회화 공부를 하고 싶지만:

- 제대로 시간을 내서 하기엔 **다른 일정에 항상 밀린다.**
- 기존 회화 앱을 다운받아 봤지만 **푸시 알림까지 무시하게 된다.** 알림 → 앱 열기 → 콘텐츠 선택 → 시작, 이 여러 단계의 마찰이 습관 형성을 막는다.
- 원하는 건 **아침에 일어나자마자, 화면을 거의 보지 않고도** 회화 연습이 시작되는 경험이다.

**핵심 가설**: 시작 마찰을 "알림 탭 1회"로 줄이고, 그 이후를 완전 핸즈프리로 만들면 아침 회화가 습관으로 정착된다.

**이름의 의미**: 까치(magpie)는 아침에 찾아와 좋은 소식을 전하는 새. 매일 아침 까치가 대화를 걸어온다는 컨셉.

## 2. 목표 / 비목표

### 목표 (MVP 기준)

1. 매일 아침 지정 시간에 알림이 오고, **탭 1회만으로 AI 음성 대화가 자동 시작**된다.
2. 대화는 **약 10분간 자연스럽게 이어지며**, 사용자는 화면을 보거나 터치할 필요가 없다 (완전 핸즈프리).
3. 하루 대화를 완료하면 **보상(코인/아이템)** 을 받아 까치의 둥지를 꾸밀 수 있다.

### 비목표 (MVP에서 하지 않는 것)

- 알림 탭 없이 알람처럼 자동으로 대화가 시작되는 기능 (iOS 정책상 일반 앱에서는 불가 → §7 제약 참조)
- 문법 교정 리포트, 단어장, 레벨 테스트 등 학습 관리 기능
- 소셜/랭킹 기능
- 수익화

## 3. 타겟 사용자

- **1차**: 본인 (한국어 화자, 프론트엔드 개발자, 아침 루틴에 회화 연습을 넣고 싶은 직장인)
- 확장 타겟은 미언급 — 개인 프로젝트로서 본인의 습관 정착이 성공 기준.

## 4. 핵심 사용자 플로우

```
[전날 밤]  앱에서 아침 알림 시간 설정 (예: 07:00)
    ↓
[아침]     📱 알림 도착: "까치가 왔어요! 오늘의 대화를 시작할까요?"
    ↓
[탭 1회]   앱 실행 → 마이크·스피커 자동 활성화 → AI가 먼저 인사/질문
    ↓
[~10분]    완전 핸즈프리 대화 (침대에 누운 채로, 화면 안 봐도 됨)
           - AI가 질문 → 사용자가 말함 → AI가 반응하고 이어감
           - 사용자가 말을 멈추면 AI가 자연스럽게 다음 화제로
    ↓
[종료]     10분 경과 시 AI가 대화를 자연스럽게 마무리
    ↓
[보상]     🪙 코인/아이템 지급 → 까치 둥지 꾸미기 화면
```

## 5. 기능 요구사항

### F1. 알림 트리거 & 자동 대화 시작

| ID | 요구사항 | 우선순위 |
|---|---|---|
| F1-1 | 사용자가 지정한 시간에 매일 반복되는 로컬 알림 발송 | P0 |
| F1-2 | 알림 탭 시 앱이 열리면서 별도 조작 없이 즉시 대화 세션 시작 (마이크 권한은 최초 1회만 요청) | P0 |
| F1-3 | 무음 모드/저음량 상태에서도 대화가 들리도록 오디오 세션 설정 (스피커 기본, 이어폰 연결 시 이어폰) | P0 |
| F1-4 | 알림을 놓쳐도 앱을 직접 열어 "오늘의 대화 시작" 버튼으로 동일 세션 진입 가능 | P1 |
| F1-5 | 알림 문구를 매일 다르게 (까치의 인사말 로테이션) | P2 |

### F2. 핸즈프리 AI 대화

| ID | 요구사항 | 우선순위 |
|---|---|---|
| F2-1 | AI가 먼저 인사하고 첫 질문을 던짐 (사용자 발화 대기 X) | P0 |
| F2-2 | 실시간 speech-to-speech 대화. 사용자가 말을 마치면 AI가 자동으로 응답 — 버튼 조작 없음 | P0 |
| F2-3 | 사용자가 일정 시간(예: 8초) 말이 없으면 AI가 힌트를 주거나 더 쉬운 질문으로 전환 | P1 |
| F2-4 | 세션 시작 후 약 10분 경과 시 AI가 대화를 자연스럽게 마무리 (갑자기 끊지 않음) | P0 |
| F2-5 | 대화 주제: 아침에 어울리는 스몰토크 (오늘 계획, 어제 있었던 일, 날씨, 가벼운 질문) | P0 |
| F2-6 | 사용자가 한국어로 말하면 AI가 영어 표현을 알려주고 따라 말하도록 유도 | P1 |
| F2-7 | 대화 난이도 설정 (예: AI 말 속도, 어휘 수준 2~3단계) | P2 |
| F2-8 | 대화 종료 후 오늘 대화의 핵심 표현 2~3개를 텍스트로 요약 표시 | P2 |

### F3. 보상 시스템 — 까치의 둥지

> 상세 메커니즘은 미정. 아래는 후보 옵션 (§10-3에서 결정 필요).

| ID | 요구사항 | 우선순위 |
|---|---|---|
| F3-1 | 하루 대화(10분 세션) 완료 시 보상 지급 | P0 |
| F3-2 | 둥지 꾸미기 화면: 획득한 아이템을 배치해 나만의 둥지를 만듦 | P0 |
| F3-3 | 연속 출석(스트릭)에 따른 추가 보상 | P1 |

**보상 메커니즘 후보:**

- **옵션 A — 코인 → 상점**: 세션당 코인 지급, 상점에서 둥지 아이템(반짝이는 물건, 리본, 나뭇가지 등) 구매. *장점: 선택의 재미. 단점: 상점 UI 추가 개발.*
- **옵션 B — 랜덤 아이템 드롭**: 세션 완료 시 까치가 "물어온" 아이템을 랜덤 지급. *장점: 개발 단순, 수집 재미(까치=반짝이는 걸 물어오는 새, 컨셉 부합). 단점: 원하는 아이템을 못 얻는 답답함.*
- **옵션 C — 스트릭 = 둥지 성장**: 연속 일수에 따라 둥지가 단계적으로 커지고 알→새끼 까치 부화. *장점: 습관 형성과 가장 직결. 단점: 하루 놓쳤을 때 이탈 리스크(둥지 퇴화 여부 설계 필요).*

MVP 추천: **B(랜덤 드롭) + 간단한 스트릭 카운터**. 상점 없이 시작해 개발량 최소화.

### F4. 설정

| ID | 요구사항 | 우선순위 |
|---|---|---|
| F4-1 | 알림 시간 설정 (요일별 on/off 포함) | P0 |
| F4-2 | 대화 길이 설정 (기본 10분, 5/10/15분 선택) | P2 |

## 6. 기술 아키텍처

> ⚙️ 용어 미니 노트 (프론트엔드 기준 비유 포함)
> - **speech-to-speech 모델**: 오디오를 넣으면 오디오가 바로 나오는 AI. 예전 방식은 STT(음성→텍스트 변환) → LLM(텍스트 생성) → TTS(텍스트→음성 합성) 3단을 직접 이어붙여야 했고 지연이 1.5초 이상이었는데, 통합 모델은 300ms~ 수준.
> - **WebSocket / WebRTC**: 둘 다 실시간 양방향 통신. WebSocket은 `fetch`가 "한 번 요청-응답"이라면 "계속 열려있는 전화선" 같은 것. WebRTC는 원래 화상통화용 프로토콜로, 오디오 스트리밍에 최적화되어 있고 네트워크가 불안정할 때 더 강함.
> - **VAD (Voice Activity Detection)**: 사용자가 말을 시작/끝냈는지 자동 감지하는 기술. "버튼 없이 대화"의 핵심. Realtime API들에 내장되어 있음.
> - **ephemeral token(임시 토큰)**: API 키를 앱에 직접 넣으면 디컴파일로 유출되므로, 서버가 수명이 짧은 1회용 토큰을 발급해주고 앱은 그걸로 AI 서버에 직접 연결하는 패턴. `.env`를 클라이언트에 노출하면 안 되는 것과 같은 이유.

### 6.1 클라이언트

- **React Native + Expo** (TypeScript)
  - 선정 이유: React 경험 그대로 이월, iOS/Android 동시 대응, 알림·오디오 요구사항 모두 라이브러리로 커버 가능
  - 주요 모듈: `expo-notifications`(로컬 알림), 오디오 세션 설정용 config plugin, WebRTC/WebSocket 클라이언트
- 상태관리: Zustand (익숙한 도구 사용)
- 로컬 저장: 스트릭/아이템/설정은 기기 로컬 저장으로 시작 (서버 DB 없이 MVP 가능)

### 6.2 음성 대화 엔진 (2026-08 기준 비교)

| | Gemini Live API (Gemini 3.1 Flash Live) | OpenAI Realtime API (gpt-realtime-2.x) |
|---|---|---|
| 방식 | speech-to-speech, WebSocket | speech-to-speech, WebRTC/WebSocket 지원 |
| 비용 | 크게 저렴 (자릿수 차이), 무료 티어 있음 | 상대적으로 고가 |
| 언어 | 90+개 언어 (한국어 혼용 대화에 유리) | 상대적으로 제한적 |
| 강점 | 가격, 응답 속도, 멀티모달 | 긴 세션(60분), 복잡한 도구 호출, WebRTC 내장 |
| 리스크 | Google 프로토콜 락인 | 비용이 개인 프로젝트에 부담 |

**MVP 추천: Gemini Live API.** 하루 10분 × 개인 사용이면 무료 티어~저비용으로 운영 가능하고, F2-6(한국어 → 영어 유도)에 다국어 지원이 유리. 클라이언트 코드를 엔진 교체가 쉽게 어댑터 패턴으로 감싸두면 나중에 갈아탈 수 있음.

### 6.3 백엔드 (최소한만)

- MVP에 필요한 서버는 **ephemeral token 발급 엔드포인트 하나뿐**. Vercel/Cloudflare Workers 서버리스 함수 1개로 충분.
- 시스템 프롬프트(까치 페르소나, 대화 규칙, 10분 마무리 지시)도 이 서버에서 내려주면 앱 업데이트 없이 대화 스타일 수정 가능.
- 사용자 계정/DB는 MVP에서 생략 (로컬 저장).

### 6.4 대화 세션 로직

```
알림 탭
 → 앱 포그라운드 진입, 오디오 세션 활성화
 → 서버에서 임시 토큰 + 시스템 프롬프트 수신
 → Live API 연결, AI가 먼저 발화 (F2-1)
 → [대화 루프] VAD 기반 턴 교환
 → 8분 경과: 시스템이 "슬슬 마무리하라"는 신호를 모델에 주입
 → 10분 전후: AI가 작별 인사 → 세션 종료
 → 보상 지급 → 둥지 화면
```

## 7. 플랫폼 제약 & 대응

| 제약 | 내용 | 대응 |
|---|---|---|
| iOS: 알림만으로 자동 실행 불가 | 일반 푸시/로컬 알림은 사용자가 탭해야 앱이 실행됨. 알람류 자동 재생은 시스템 앱/특수 권한 영역 | 제품 요구사항을 "탭 1회 후 핸즈프리"로 정의 (결정 완료) |
| iOS: 백그라운드 오디오 | 화면이 꺼지거나 다른 앱으로 가면 오디오가 끊길 수 있음 | `audio` background mode 활성화 → 대화 중 화면 꺼져도 계속 진행 (침대에서 눈 감고 가능) |
| 무음 모드 | 아침엔 무음 모드일 확률 높음 | 오디오 세션 카테고리를 playback으로 설정해 무음 스위치 무시 |
| 마이크 권한 | 최초 1회 시스템 팝업 필수 | 온보딩(첫 실행)에서 미리 받아두기 — 첫 아침에 팝업이 뜨면 "핸즈프리" 경험이 깨짐 |
| Android 파편화 | 제조사별 배터리 최적화가 알림을 지연시킬 수 있음 | 정확한 알람 권한(exact alarm) 사용, 설정 가이드 화면 제공 |
| 네트워크 | 아침에 Wi-Fi 미연결 등으로 연결 실패 가능 | 연결 실패 시 음성 안내 + 재시도, 반복 실패 시 "오늘은 쉬어가기" 처리(스트릭 보호 여부는 §10) |

## 8. MVP 범위 & 마일스톤

**M1 — 대화 코어 (가장 리스크 큰 것부터)**
Expo 프로젝트 셋업 → Gemini Live 연결 → 버튼 눌러 10분 핸즈프리 대화가 되는지 검증. *여기가 되면 절반은 된 것.*

**M2 — 알림 트리거**
로컬 알림 예약 → 탭 시 자동 세션 시작 → 무음 모드/화면 꺼짐/이어폰 시나리오 테스트.

**M3 — 보상 & 둥지**
세션 완료 감지 → 아이템 드롭 → 둥지 꾸미기 화면 (간단한 2D 배치).

**M4 — 본인 실사용 (2주)**
매일 아침 실제로 쓰면서 대화 품질·알림 신뢰성 튜닝. 성공 지표 측정.

## 9. 성공 지표 (개인 프로젝트 기준)

- **핵심**: 2주간 주 5회 이상 아침 세션 완료 (습관 정착 여부)
- 알림 탭 → AI 첫 발화까지 5초 이내 (마찰 체감 기준)
- 세션 중도 이탈률 (10분을 못 채우고 끄는 비율) — 대화 품질의 프록시
- 정성 평가: "화면을 안 보고도 대화가 됐는가", "내일도 하고 싶은가"

## 10. 미결정 사항 (구현하며 결정)

1. **보상 메커니즘 확정** — 옵션 A/B/C 중 선택 (§5-F3, MVP 추천은 B+스트릭)
2. **까치 페르소나 톤** — 친근한 친구 vs 선생님 vs 귀여운 캐릭터 말투. 시스템 프롬프트로 결정, 실사용하며 튜닝
3. **스트릭이 끊겼을 때 처리** — 둥지 퇴화 없음(순한 맛) vs 프리즈 아이템(듀오링고식) vs 그냥 리셋
4. **대화 난이도 초기값** — 본인 레벨 기준으로 시스템 프롬프트에 하드코딩할지, 설정 UI를 만들지 (F2-7은 P2)
5. **한국어 발화 허용 정도** — 완전 영어 강제 vs 막히면 한국어로 물어보기 허용 (F2-6)
6. **네트워크 실패일의 스트릭 보호 여부** (§7)
7. **둥지 그래픽 소스** — 직접 그리기 vs AI 생성 vs 에셋 구매
8. **10분의 정의** — 정확히 10분 컷 vs 8~12분 사이 자연 종료 (후자 추천)
